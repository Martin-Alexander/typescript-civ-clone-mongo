<h1>"Game": <%= @game.id %></h1>

<ul>
  <% @players.each do |player| %>
    <li>
      <div><%= player.user.username %></div>
      <div><%= player.role %></div>
      <div><%= player.host ? "host" : "" %></div>
    </li>
  <% end %>
</ul>